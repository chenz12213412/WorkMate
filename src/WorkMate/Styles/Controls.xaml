<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:sys="clr-namespace:System;assembly=System.Runtime">
  <sys:Double x:Key="SpacingXS">4</sys:Double>
  <sys:Double x:Key="SpacingS">8</sys:Double>
  <sys:Double x:Key="SpacingM">12</sys:Double>
  <sys:Double x:Key="SpacingL">16</sys:Double>
  <sys:Double x:Key="SpacingXL">24</sys:Double>
  <sys:Double x:Key="DashboardWidth">620</sys:Double>
  <sys:Double x:Key="DashboardHeight">800</sys:Double>
  <sys:Double x:Key="DashboardMinWidth">600</sys:Double>
  <sys:Double x:Key="DashboardMinHeight">760</sys:Double>
  <sys:Double x:Key="IconSizeSmall">20</sys:Double>
  <sys:Double x:Key="IconSizeMedium">24</sys:Double>
  <sys:Double x:Key="IconSizeLarge">32</sys:Double>
  <sys:Double x:Key="StateCardHeight">170</sys:Double>
  <sys:Double x:Key="StateIconSize">104</sys:Double>
  <sys:Double x:Key="MetricIconBoxSize">34</sys:Double>
  <sys:Double x:Key="ActionIconBoxSize">30</sys:Double>
  <sys:Double x:Key="NavigationIconBoxSize">22</sys:Double>
  <sys:Double x:Key="SettingsGearIconBoxSize">22</sys:Double>
  <sys:Double x:Key="MetricsGridHeight">190</sys:Double>
  <sys:Double x:Key="MetricCardHeight">91</sys:Double>
  <sys:Double x:Key="MetricCardMinHeight">91</sys:Double>
  <sys:Double x:Key="ChartCardHeight">190</sys:Double>
  <sys:Double x:Key="ChartCardMinHeight">140</sys:Double>
  <sys:Double x:Key="ActionsGridHeight">130</sys:Double>
  <sys:Double x:Key="ActionButtonHeight">61</sys:Double>
  <sys:Double x:Key="FooterHeight">46</sys:Double>
  <sys:Double x:Key="NavigationItemHeight">50</sys:Double>
  <CornerRadius x:Key="CornerRadiusSmall">6</CornerRadius>
  <CornerRadius x:Key="CornerRadiusMedium">10</CornerRadius>
  <CornerRadius x:Key="CornerRadiusLarge">14</CornerRadius>
  <Thickness x:Key="CardPadding">16</Thickness>

  <Style TargetType="Image">
    <Setter Property="RenderOptions.BitmapScalingMode" Value="HighQuality" />
    <Setter Property="SnapsToDevicePixels" Value="True" />
    <Setter Property="Stretch" Value="Uniform" />
  </Style>

  <Style x:Key="CardStyle" TargetType="Border">
    <Setter Property="Background" Value="{StaticResource SurfaceBrush}" />
    <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}" />
    <Setter Property="BorderThickness" Value="1" />
    <Setter Property="CornerRadius" Value="{StaticResource CornerRadiusMedium}" />
    <Setter Property="Padding" Value="{StaticResource CardPadding}" />
  </Style>

  <Style x:Key="MetricCardStyle" TargetType="Border" BasedOn="{StaticResource CardStyle}">
    <Setter Property="Background" Value="{StaticResource SurfaceMutedBrush}" />
    <Setter Property="BorderThickness" Value="0" />
    <Setter Property="Padding" Value="14" />
    <Setter Property="MinHeight" Value="{StaticResource MetricCardMinHeight}" />
  </Style>

  <Style x:Key="SettingsGearButtonStyle" TargetType="Button">
    <Setter Property="Width" Value="36" />
    <Setter Property="Height" Value="36" />
    <Setter Property="Padding" Value="8" />
    <Setter Property="Background" Value="Transparent" />
    <Setter Property="BorderThickness" Value="0" />
    <Setter Property="FocusVisualStyle" Value="{x:Null}" />
    <Setter Property="Cursor" Value="Hand" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="Button">
          <Border x:Name="ButtonBorder" Background="{TemplateBinding Background}"
                  BorderBrush="{StaticResource PrimaryBrush}" BorderThickness="0"
                  CornerRadius="{StaticResource CornerRadiusSmall}"
                  Padding="{TemplateBinding Padding}">
            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
          </Border>
          <ControlTemplate.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
              <Setter TargetName="ButtonBorder" Property="Background" Value="{StaticResource SurfaceSelectedBrush}" />
            </Trigger>
            <Trigger Property="IsPressed" Value="True">
              <Setter TargetName="ButtonBorder" Property="Background" Value="{StaticResource BorderBrush}" />
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <Style x:Key="ActionButtonStyle" TargetType="Button">
    <Setter Property="FontFamily" Value="{StaticResource AppFontFamily}" />
    <Setter Property="FontSize" Value="13" />
    <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}" />
    <Setter Property="Background" Value="{StaticResource SurfaceBrush}" />
    <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}" />
    <Setter Property="BorderThickness" Value="1" />
    <Setter Property="Padding" Value="10,8" />
    <Setter Property="MinHeight" Value="40" />
    <Setter Property="Cursor" Value="Hand" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="Button">
          <Border x:Name="ButtonBorder" Background="{TemplateBinding Background}"
                  BorderBrush="{TemplateBinding BorderBrush}"
                  BorderThickness="{TemplateBinding BorderThickness}"
                  CornerRadius="{StaticResource CornerRadiusMedium}"
                  Padding="{TemplateBinding Padding}">
            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
          </Border>
          <ControlTemplate.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
              <Setter TargetName="ButtonBorder" Property="Background" Value="{StaticResource SurfaceSelectedBrush}" />
            </Trigger>
            <Trigger Property="IsPressed" Value="True">
              <Setter TargetName="ButtonBorder" Property="Background" Value="{StaticResource BorderBrush}" />
            </Trigger>
            <Trigger Property="IsKeyboardFocused" Value="True">
              <Setter TargetName="ButtonBorder" Property="BorderBrush" Value="{StaticResource PrimaryBrush}" />
              <Setter TargetName="ButtonBorder" Property="BorderThickness" Value="2" />
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
              <Setter Property="Opacity" Value="0.45" />
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <Style x:Key="DashboardActionButtonStyle" TargetType="Button"
         BasedOn="{StaticResource ActionButtonStyle}">
    <Setter Property="FontSize" Value="{StaticResource FontSizeAction}" />
    <Setter Property="MinHeight" Value="{StaticResource ActionButtonHeight}" />
    <Setter Property="Padding" Value="12,10" />
  </Style>

  <Style x:Key="NavigationButtonStyle" TargetType="RadioButton">
    <Setter Property="FontFamily" Value="{StaticResource AppFontFamily}" />
    <Setter Property="FontSize" Value="15" />
    <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}" />
    <Setter Property="Background" Value="Transparent" />
    <Setter Property="Padding" Value="14,11" />
    <Setter Property="MinHeight" Value="{StaticResource NavigationItemHeight}" />
    <Setter Property="Cursor" Value="Hand" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="RadioButton">
          <Border x:Name="NavigationBorder" Background="{TemplateBinding Background}"
                  CornerRadius="{StaticResource CornerRadiusSmall}"
                  Padding="{TemplateBinding Padding}">
            <ContentPresenter VerticalAlignment="Center" />
          </Border>
          <ControlTemplate.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
              <Setter TargetName="NavigationBorder" Property="Background" Value="{StaticResource SurfaceMutedBrush}" />
            </Trigger>
            <Trigger Property="IsChecked" Value="True">
              <Setter TargetName="NavigationBorder" Property="Background" Value="{StaticResource SurfaceSelectedBrush}" />
              <Setter Property="Foreground" Value="{StaticResource PrimaryBrush}" />
              <Setter Property="FontWeight" Value="SemiBold" />
            </Trigger>
            <Trigger Property="IsKeyboardFocused" Value="True">
              <Setter TargetName="NavigationBorder" Property="BorderBrush" Value="{StaticResource PrimaryBrush}" />
              <Setter TargetName="NavigationBorder" Property="BorderThickness" Value="1" />
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>
</ResourceDictionary>
